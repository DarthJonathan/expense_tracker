server:
  port: "8080"
  logLevel: "info"

database:
  url: "postgres://postgres:postgres@localhost:5432/expense_tracker?sslmode=disable"
  schema: "spendit"

cors:
  allowOrigin: "http://localhost:3000"

auth:
  jwtSecret: "local-dev-secret"
  tokenExpiryHours: 168
